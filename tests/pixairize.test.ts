import { pixairize } from '../src/pixairize';

it('Runs without crashing', () => {

    // GIVEN
    // givenPageWithNoImages();
    // const pixairReplaceSpy = spyOn(pixairReplace);

    // WHEN
    pixairize();

    // THEN
    // expect(pixairReplaceSpy).not.toHaveBeenCalled();
});
